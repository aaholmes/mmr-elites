[package]
name = "mmr_elites_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "mmr_elites_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
numpy = "0.20"
ndarray = "0.15"  # Standard Rust tensor library
